name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  workflow_dispatch:

jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    permissions:
     issues: write  # 明确授予创建 issue 的权限
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      
      - name: Show the CI workflow URL
        run: ｜
            cd /aa

      - name: Create Issue on Failure
        if: failure()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh issue create \
            --title "Workflow Failed: ${{ github.workflow }} #${{ github.run_number }}" \
            --body "The workflow failed on $(date +'%Y-%m-%d %H:%M:%S'). See run: [${{ github.run_id }}](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})" \

